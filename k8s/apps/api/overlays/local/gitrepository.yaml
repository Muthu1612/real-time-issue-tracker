apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: real-time-issue-tracker
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/<your-org>/real-time-issue-tracker.git
  ref:
    branch: main